@model IEnumerable<fitness.Models.GoalProgess>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CurrentWeight)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CurrentHeight)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Goal.UserId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CurrentWeight)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CurrentHeight)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Goal.UserId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
